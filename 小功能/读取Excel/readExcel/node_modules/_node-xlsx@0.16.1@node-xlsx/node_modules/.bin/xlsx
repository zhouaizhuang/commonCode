#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_xlsx@0.16.9@xlsx/bin/xlsx.njs" "$@"
  ret=$?
else 
  node  "$basedir/../../../_xlsx@0.16.9@xlsx/bin/xlsx.njs" "$@"
  ret=$?
fi
exit $ret
